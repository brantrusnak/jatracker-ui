import React from 'react';
import { SigninForm } from './SigninForm/SigninForm';

export const Signin: React.FC = () => {
    return (
        <div>
            <SigninForm />
        </div>
    );
};